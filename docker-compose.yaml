services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: minnatropolis
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
